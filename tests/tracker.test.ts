import test from 'node:test'; import assert from 'node:assert/strict'; import { mkdtemp, rm } from 'node:fs/promises'; import os from 'node:os'; import path from 'node:path'; import * as XLSX from 'xlsx';
import { ExcelTracker } from '../src/tracker/excel-tracker.js';
test('claims only one pending tracker row while preserving its length and type', async () => { const dir = await mkdtemp(path.join(os.tmpdir(), 'wp-blog-agent-')); const file = path.join(dir, 'tracker.xlsx'); try { const book = XLSX.utils.book_new(); const sheet = XLSX.utils.aoa_to_sheet([['blog_id', 'blog_topic', 'blog_length', 'blog_type', 'blog_status', 'blog_created_date', 'blog_posted_date'], ['2', 'Local-first WordPress', 500, 'short', 'pending', '', ''], ['3', 'Already waiting', 1500, 'long', 'awaiting_review', '', '']]); XLSX.utils.book_append_sheet(book, sheet, 'Blog tracker'); XLSX.writeFile(book, file); const tracker = new ExcelTracker(file); const formats = new Set(['short', 'long']); const row = await tracker.claimNext(formats); assert.equal(row?.blog_id, '2'); assert.equal(row?.blog_length, 500); assert.equal(row?.blog_type, 'short'); assert.equal((await tracker.claimNext(formats)), undefined); await tracker.update('2', { markdown_path: 'data/drafts/2.md', blog_status: 'awaiting_review' }); const rows = await tracker.rows(); assert.equal(rows[0].blog_status, 'awaiting_review'); assert.equal(rows[0].markdown_path, 'data/drafts/2.md'); } finally { await rm(dir, { recursive: true, force: true }); } });
