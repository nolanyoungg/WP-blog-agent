import { mkdir, readFile, writeFile } from 'node:fs/promises';
import path from 'node:path';
import type { BlogRow } from '../types.js';
const slugify = (value: string) => value.toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '').slice(0, 80) || 'blog';
const splitFrontMatter = (raw: string) => { const match = raw.match(/^---\n([\s\S]*?)\n---\n?([\s\S]*)$/); return { front: match?.[1] ?? '', body: match?.[2] ?? raw }; };
export const promptFor = (topic: string) => `Create a polished, factual and original WordPress blog article about: ${topic}\n\nReturn Markdown only. Use this structure: # Title, a one-paragraph meta description, ## Introduction, multiple ## Main sections, ## Practical steps or examples, and ## Conclusion. Do not add citations unless you can support them from the prompt; do not invent facts. Include YAML front matter with title, excerpt, slug, categories, and tags.`;
export const saveDraft = async (dir: string, row: BlogRow, markdown: string, model: string) => { await mkdir(dir, { recursive: true }); const file = path.join(dir, `${row.blog_id}-${slugify(row.blog_topic)}.md`); const generated = splitFrontMatter(markdown.trim()); const provenance = `blog_id: "${row.blog_id}"\ntopic: "${row.blog_topic.replaceAll('"', '\\"')}"\ngenerated_at: "${new Date().toISOString()}"\nmodel_used: "${model}"\nreview_status: pending`; await writeFile(file, `---\n${provenance}${generated.front ? `\n${generated.front}` : ''}\n---\n\n${generated.body.trim()}\n`, 'utf8'); return file; };
export const parseDraft = async (file: string) => { const raw = await readFile(file, 'utf8'); const { front, body } = splitFrontMatter(raw); const map = new Map(front.split('\n').flatMap(line => { const index = line.indexOf(':'); return index > 0 ? [[line.slice(0, index).trim(), line.slice(index + 1).trim()]] : []; })); const value = (key: string) => (map.get(key) ?? '').replace(/^['"]|['"]$/g, ''); const list = (key: string) => value(key).replace(/^\[|\]$/g, '').split(',').map(item => item.trim()).filter(Boolean); const title = value('title') || body.match(/^#\s+(.+)$/m)?.[1] || 'Untitled'; const excerpt = value('excerpt') || body.replace(/^#.*$/m, '').trim().slice(0, 155); return { body, title, excerpt, slug: value('slug') || slugify(title), categories: list('categories'), tags: list('tags') }; };
